/**
 * @description 反转单向链表
 *
 */

interface ILinkeListNode {
  value: number;
  next?: ILinkeListNode;
  prev?: ILinkeListNode;
}

function createLinkList(arr: number[]): ILinkeListNode[] {
  return [];
}
